# Copy this file to .env and fill in your values.
# .env is git-ignored and mounted into the container at /work/.env.
# Any OpenAI-compatible endpoint works (official OpenAI, Azure, self-hosted, national models, etc.)

OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-replace-me
LLM_MODEL=gpt-4o-mini
